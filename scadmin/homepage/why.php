<?php

declare(strict_types=1);require_once __DIR__ . '/../../app/bootstrap.php';Auth::requirePermission('manage_homepage');
$id=filter_input(INPUT_GET,'id',FILTER_VALIDATE_INT);$existing=null;if($id){foreach(HomeManager::whyTabs() as $r)if((int)$r['id']===$id)$existing=$r;if(!$existing)redirect(adminUrl('homepage/'));}$error=null;if(isPost()){CSRF::verify($_POST['csrf_token']??null);try{$new=HomeManager::saveWhyTab($_POST,$id?:null);Auth::audit(Auth::id(),$id?'update':'create','home_why_tab',$new,($id?'Updated':'Created').' Why State Corps tab');flash('success','Why State Corps tab saved.');redirect(adminUrl('homepage/why.php?id='.$new));}catch(Throwable $e){$error=APP_DEBUG?$e->getMessage():'The Why State Corps tab could not be saved.';$existing=array_merge((array)$existing,$_POST);}}
$items=[];if($existing){$items=$existing['items']??[];}$pageTitle=$existing?'Edit Why State Corps Tab':'Add Why State Corps Tab';$activeNav='homepage';require __DIR__.'/../partials/header.php';require __DIR__.'/../partials/sidebar.php';
?><main class="admin-content"><?php $breadcrumbs=[['label'=>'Dashboard','url'=>adminUrl('dashboard.php')],['label'=>'Homepage','url'=>adminUrl('homepage/')],['label'=>$pageTitle,'url'=>null]];require __DIR__.'/../partials/breadcrumbs.php';$heading=$pageTitle;$description='Preserve legacy IDs because the homepage may use them in tab identifiers.';$actionUrl=adminUrl('homepage/');$actionLabel='Back';require __DIR__.'/../partials/page-heading.php';require __DIR__.'/../partials/alerts.php';if($error):?><div class="alert alert-error"><?=e($error)?></div><?php endif;?><section class="form-card"><form method="post"><?=CSRF::field()?><div class="form-grid"><div class="form-field"><label>Legacy ID</label><input name="legacy_id" value="<?=e($existing['legacy_id']??'')?>" required></div><div class="form-field"><label>Sort order</label><input name="sort_order" type="number" min="0" value="<?=e((string)($existing['sort_order']??0))?>"></div><div class="form-field"><label>Tab name</label><input name="tab_name" value="<?=e($existing['tab_name']??'')?>" required></div><div class="form-field"><label>Title</label><input name="title" value="<?=e($existing['title']??'')?>"></div><div class="form-field form-field-wide"><label>Image path</label><input name="image_path" value="<?=e($existing['image_path']??'')?>"></div></div><div class="permission-box"><h2>Tab items</h2><div id="why-items"><?php foreach($items as $i=>$item):?><div class="repeat-row single"><input name="items[]" value="<?=e($item['item_text']??$item)?>"><button type="button" class="small-button small-button-danger remove-row">Remove</button></div><?php endforeach;?></div><button type="button" class="small-button" id="add-why">+ Add item</button></div><label class="permission-option"><input type="checkbox" name="is_active" value="1" <?=!isset($existing['is_active'])||$existing['is_active']?'checked':''?>><span><strong>Active</strong></span></label><div class="form-actions"><a class="button-link button-secondary" href="<?=e(adminUrl('homepage/'))?>">Cancel</a><button class="button-primary" type="submit">Save Tab</button></div></form></section><script>document.getElementById('add-why').addEventListener('click',function(){document.getElementById('why-items').insertAdjacentHTML('beforeend','<div class="repeat-row single"><input name="items[]" value=""><button type="button" class="small-button small-button-danger remove-row">Remove</button></div>')});document.addEventListener('click',function(e){if(e.target.classList.contains('remove-row'))e.target.closest('.repeat-row').remove()});</script></main><?php require __DIR__.'/../partials/footer.php'; ?>
