Phase 16 adds a reusable global settings facade and safely seeds the first global settings used by the shared header/footer/head area.

No existing public markup is overwritten because the uploaded project structure did not include the actual PHP source of the shared partials. This avoids inventing or changing the existing design.

PHP-only, PDO/MySQL, compatible with the existing CMS architecture.
