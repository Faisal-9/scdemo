STATE CORPS — PHASE 20
Asset Library / File Upload Management

Incremental/delta package only.
No existing public content, design, HTML, CSS, JS, image paths, or PDF paths are changed.

Adds a reusable library for CMS-managed image/PDF uploads plus admin metadata editing.
The public site can continue using the existing asset paths; newly uploaded assets can be copied into existing CMS fields.
