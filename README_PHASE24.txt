Phase 24 adds a read-only System Health center to the existing CMS.
Primary URL: /scadmin/system-health/index.php

The module uses existing Database::connection(), Auth::requirePermission(), and
admin partials. It does not change the public website or existing CMS content.
